---
import Layout from "../../layouts/Layout.astro";
import { ProductGrid } from "../../components/shop/ProductGrid";
import { CartSidebar } from "../../components/shop/CartSidebar";
import { db } from "../../db";
import { productos } from "../../db/schema";

// Obtener TODOS los productos
const allProducts = await db.select().from(productos);

// Separar destacados para un showcase inicial (opcional)
const destacados = allProducts
  .filter((p) => p.destacado || p.stock < 5)
  .slice(0, 4);
---

<Layout title="Tienda Solidaria - La Banda a Jujuy">
  <!-- Hero de la tienda -->
  <div
    class="relative bg-gradient-to-r from-brand-blue to-blue-800 overflow-hidden text-white shadow-xl"
  >
    <!-- Abstract Shapes Background -->
    <div class="absolute inset-0 opacity-10">
      <svg
        class="h-full w-full"
        viewBox="0 0 100 100"
        preserveAspectRatio="none"
      >
        <path d="M0 100 C 20 0 50 0 100 100 Z" fill="white"></path>
      </svg>
    </div>

    <div
      class="container mx-auto px-4 py-16 md:py-24 relative z-10 flex flex-col md:flex-row items-center justify-between gap-8"
    >
      <div class="max-w-2xl">
        <div
          class="inline-block px-3 py-1 bg-brand-orange text-white text-xs font-bold rounded-full mb-4 uppercase tracking-wider"
        >
          100% A beneficio
        </div>
        <h1 class="text-4xl md:text-6xl font-extrabold mb-6 leading-tight">
          Tienda <span
            class="text-transparent bg-clip-text bg-gradient-to-r from-brand-orange to-brand-dark"
            >Solidaria</span
          >
        </h1>
        <p
          class="text-lg md:text-xl text-blue-100 mb-8 leading-relaxed max-w-lg"
        >
          Comprá productos oficiales, vales de comida y artesanías locales.
          <strong class="text-white"
            >Todo lo recaudado nos acerca a Jujuy.</strong
          >
        </p>

        <!-- Stats mini -->
        <div class="flex flex-wrap gap-4 text-sm font-medium">
          <div
            class="flex items-center gap-2 bg-white/10 backdrop-blur-md px-4 py-2 rounded-full border border-white/20"
          >
            <span>🎁</span>
            <span>{allProducts.length} productos</span>
          </div>
          <div
            class="flex items-center gap-2 bg-white/10 backdrop-blur-md px-4 py-2 rounded-full border border-white/20"
          >
            <span>🛡️</span>
            <span>Compra Segura</span>
          </div>
        </div>
      </div>

      <!-- Optional: Hero Image or Icon -->
      <div class="hidden md:block opacity-80 mix-blend-overlay">
        <span class="text-9xl">🛍️</span>
      </div>
    </div>
  </div>

  <!-- Contenido principal -->
  <main class="container mx-auto px-4 py-12 -mt-8 relative z-20">
    <!-- Product Grid con todo integrado (Filtros, Búsqueda, Grid) -->
    <!-- Pasamos 'destacados' si quisieramos mostrarlos diferente, pero el Grid ya maneja todo -->
    <div class="bg-white rounded-3xl shadow-2xl p-6 md:p-8 min-h-[500px]">
      <ProductGrid client:load products={allProducts} />
    </div>

    <!-- Info importante / Footer de la tienda -->
    <div class="mt-16 grid grid-cols-1 md:grid-cols-3 gap-6 opacity-80">
      <div
        class="flex items-start gap-4 p-4 rounded-xl hover:bg-gray-50 transition-colors"
      >
        <div class="text-3xl bg-blue-50 p-3 rounded-lg">🚚</div>
        <div>
          <h4 class="font-bold text-gray-900">Envíos y Retiros</h4>
          <p class="text-sm text-gray-600 mt-1">
            Coordinamos la entrega en La Banda o retirás en el club.
          </p>
        </div>
      </div>
      <div
        class="flex items-start gap-4 p-4 rounded-xl hover:bg-gray-50 transition-colors"
      >
        <div class="text-3xl bg-green-50 p-3 rounded-lg">💳</div>
        <div>
          <h4 class="font-bold text-gray-900">Medios de Pago</h4>
          <p class="text-sm text-gray-600 mt-1">
            Aceptamos todas las tarjetas con MercadoPago y efectivo.
          </p>
        </div>
      </div>
      <div
        class="flex items-start gap-4 p-4 rounded-xl hover:bg-gray-50 transition-colors"
      >
        <div class="text-3xl bg-orange-50 p-3 rounded-lg">🤝</div>
        <div>
          <h4 class="font-bold text-gray-900">Impacto Real</h4>
          <p class="text-sm text-gray-600 mt-1">
            Cada peso va a una cuenta transparente para el viaje.
          </p>
        </div>
      </div>
    </div>
  </main>

  <!-- Carrito Sidebar -->
  <CartSidebar client:load />

  <!-- Floating CTA Rifas -->
  <div class="fixed bottom-6 right-6 z-40">
    <a
      href="/rifas"
      class="group flex items-center gap-3 pl-4 pr-6 py-3 bg-gray-900 text-white font-bold rounded-full shadow-2xl hover:shadow-gray-900/50 hover:-translate-y-1 transition-all duration-300"
    >
      <span class="text-2xl group-hover:rotate-12 transition-transform">🎟️</span
      >
      <div class="flex flex-col items-start leading-none">
        <span class="text-[10px] text-gray-400 uppercase tracking-wider"
          >Participá</span
        >
        <span>Ver Rifas</span>
      </div>
    </a>
  </div>
</Layout>
